target/
# Anchor to the crate root so the library's lockfile stays untracked
# but `fuzz/Cargo.lock` (conventional for a cargo-fuzz crate, which is
# a separate non-published binary member) can be committed.
/Cargo.lock
